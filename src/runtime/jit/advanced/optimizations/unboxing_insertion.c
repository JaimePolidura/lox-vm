#include "unboxing_insertion.h"

struct ui {
    struct ssa_ir * ssa_ir;
    struct u64_hash_table 
};

void perform_unboxing_insertion(struct ssa_ir * ssa_ir) {
    for_each_ssa_block(ssa_ir->first_block, );
}